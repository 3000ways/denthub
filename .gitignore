node_modules/
.next/
.DS_Store
*.webp
.vercel

# Never commit env files — real secrets (service-role key, AIRTABLE_PAT, etc.) must
# live in Vercel only. GitHub secret scanning misses most of these formats.
.env
.env.*
!.env.example
.env*

# Final Airtable CSV exports (backup only — contain submitter emails, never commit)
Resources-Grid view.csv
Categories-Grid view.csv
